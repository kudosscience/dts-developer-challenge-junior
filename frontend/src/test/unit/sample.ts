/* eslint-disable jest/expect-expect */
describe('Example test to satisfy jest (to be removed from app)', () => {
  test('to be removed from app', async () => {
    // eslint-disable-line @typescript-eslint/no-empty-function
  });
});
